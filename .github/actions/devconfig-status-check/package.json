{
  "name": "devconfig-status-check",
  "version": "1.0.0",
  "description": "Checks DevConfig labels and blocks PR if requirements not met",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0"
  },
  "keywords": [
    "github-actions",
    "devconfig",
    "aws-sdk-net",
    "status-check"
  ],
  "author": "AWS SDK Team",
  "license": "Apache-2.0"
}
